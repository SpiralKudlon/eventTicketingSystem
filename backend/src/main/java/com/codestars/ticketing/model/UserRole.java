package com.codestars.ticketing.model;

/**
 * UserRole Enum - Simple role-based access control
 * Assignment 14: Simplified approach without separate Role entity
 */
public enum UserRole {
    USER,   // Regular customers who can purchase tickets
    ADMIN   // Administrators (future use for event management)
}
